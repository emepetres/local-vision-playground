# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Frames the camera took, kept so a surprising Observation can be explained afterwards
vision/frames/

# The JSON Lines a Watch emits with --emit, one file per Watch
vision/observations.jsonl

# .NET
bin/
obj/
